#include<stdio.h>
#include<conio.h>

#define p printf
#define s scanf

main() {
	int sum, num;
	clrscr();

	p("Enter Any Number to Get Sum of : ");
	s("%d",&num);

	sum = num * (num + 1) / 2;

	p("Sum of %d = %d", num, sum);

	getch();
}